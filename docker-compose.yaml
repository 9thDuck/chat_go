services:
  db:
    image: postgres
    container_name: postgres_db
    environment:
      POSTGRES_DB: chat_rg_pq
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin_password
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  db-data: